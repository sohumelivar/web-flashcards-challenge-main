const React = require('react');
const Layout = require('./Layout');

module.exports = function Profile({ title }) {
  return (
    <Layout title={title}>
      <div>
        HELLO
      </div>
    </Layout>
  );
};
